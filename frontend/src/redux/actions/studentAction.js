import {
    GET_STUDENT_DETAILS_REQUEST,
    GET_STUDENT_DETAILS_SUCCESS,
    GET_STUDENT_DETAILS_FAIL,
} from "../constants/studentConstant";
import axios from "axios";

const getStudentDetails = () => async (dispatch, getState) => {
    try {
    } catch (error) {}
};
